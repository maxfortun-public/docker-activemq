#!/bin/bash -ex

VERSIONED_IMAGE_NAME=${IMAGE_NAME/:latest/:$ACTIVEMQ_VERSION}

docker build --build-arg ACTIVEMQ_VERSION=$ACTIVEMQ_VERSION -f $DOCKERFILE_PATH -t $IMAGE_NAME -t $VERSIONED_IMAGE_NAME .


